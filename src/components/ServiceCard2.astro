---
import { Icon } from 'astro-icon/components';

const { service, index } = Astro.props;
---

<div
  class={`flex flex-col md:flex-row bg-white shadow-lg gap-10 ${index % 2 === 0 ? 'md:flex-row-reverse' : ''}`}
>
  <div class='flex flex-col gap-4 justify-center px-4 md:px-6 flex-1'>
    <Icon name={service.icon} size={40} class='text-darkBlue' />
    <h3 class='inline-block text-darkBlue text-2xl font-semibold'>
      {service.name}
    </h3>
    <p class='text-darkText text-lg leading-relaxed'>
      {service.description}
    </p>
    <a
      href='/services'
      class='flex items-center uppercase text-darkText font-medium hover:text-primary transition-colors'
      aria-label={`Learn more about ${service.name}`}
    >
      Find Out More
      <div
        class='bg-primary ml-2 p-2 rounded-full hover:bg-darkBlue transition'
      >
        <Icon name='arrow-right' size={18} class='text-lightGray' />
      </div>
    </a>
  </div>
  <img
    src={service.image}
    alt={service.name}
    class='h-auto w-full md:h-[400px] md:w-1/2 object-cover'
  />
</div>
