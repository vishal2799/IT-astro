<section class='py-16 px-5 text-center'>
  <div class='grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto'>
    <div class='p-8'>
      <h3
        id='stat-1'
        class='text-6xl font-bold text-secondary mb-4'
        data-target='50'
        data-suffix='+'
      >
        0
      </h3>
      <p class='text-lg font-medium text-textPrimary'>Projects Completed</p>
    </div>
    <div class='p-8'>
      <h3
        id='stat-2'
        class='text-6xl font-bold text-secondary mb-4'
        data-target='5'
        data-suffix='+'
      >
        0
      </h3>
      <p class='text-lg font-medium text-textPrimary'>Years of Experience</p>
    </div>
    <div class='p-8'>
      <h3
        id='stat-3'
        class='text-6xl font-bold text-secondary mb-4'
        data-target='15'
        data-suffix='+'
      >
        0
      </h3>
      <p class='text-lg font-medium text-textPrimary'>Different Domains</p>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const counters = document.querySelectorAll('h3[data-target]');

    counters.forEach((counter: any) => {
      const target = +counter.getAttribute('data-target');
      const suffix = counter.getAttribute('data-suffix') || '';
      const speed = 50; // Lower number = faster animation

      const updateCount = () => {
        const current = +counter.innerText;

        // Calculate increment
        const increment = Math.ceil(target / 100);

        if (current < target) {
          counter.innerText = current + increment;
          setTimeout(updateCount, speed);
        } else {
          counter.innerText = target + suffix; // Add suffix after completion
        }
      };

      updateCount();
    });
  });
</script>
